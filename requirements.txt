# Core dependencies
apify>=2.0.0
apify-client>=1.7.0

# Agent SDK (optional - not used in MVP)
# claude-agent-sdk>=0.1.0

# Gemini API (CRITICAL: Use unified SDK, not legacy package)
google-genai>=0.3.0  # Unified SDK with File Search support
# NOT: google-generativeai (legacy package, incompatible with File Search)

# Document processing
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Utilities
python-dateutil>=2.8.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
